using System;
using System.Collections.Generic;
using System.Linq;

namespace Hearts.Extensions
{
    /*public static class ArrayExtensions
    {
        public static void Add<T>(this T[] self, T element)
        {
            var elements = self.ToList<T>();
            elements.Add(element);
            self = elements.ToArray<T>();
        }

        public static void AddRange<T>(this T[] self, IEnumerable<T> elements)
        {
            var elements2 = self.ToList<T>();
            elements2.AddRange(elements);
            self = elements2.ToArray<T>();
        }

        public static void Remove<T>(this T[] self, T element)
        {
            var elements = self.ToList<T>();
            elements.Remove(element);
            self = elements.ToArray<T>();
        }
    }*/
}